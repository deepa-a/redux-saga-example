import React from 'react';
import { shallow, mount } from 'enzyme';
import Sidebar from './Sidebar';
import Header from './Header';
import App from './App';

it('Show a header', () => {
  // console.log(wrapped.find(Sidebar));
  // expect(wrapped.find(Header).length).toEqual(1);
});
